import ReactDOM from "react-dom";
import App from "./App"
import React from "react";

ReactDOM.render(<App/>,document.getElementById("root"))
// The responsibility of index.js is just to render the components like "App"
